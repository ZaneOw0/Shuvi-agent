# 02 平台基线

> 本文是工程平台、SDK 与工具链版本的统一记录。项目目标见 [00-项目总览](00-项目总览.md)，协作规则见 [01-协作与工程约定](01-协作与工程约定.md)。
> 本次通过只读检查工程配置、安装元数据与既有报告核验，未执行构建、安装或设备测试。

## 1. 基线状态与适用范围

项目计划使用 ArkTS / ArkUI、DevEco Studio 和 DevEco Code，面向开源鸿蒙社区竞赛开发。

当前工程配置的运行系统为 **HarmonyOS**，本机已安装的配套 SDK 为 **HarmonyOS 26.0.0，API 26**。赛事要求的 OpenHarmony 发行版、目标设备及可用接口范围仍需确认，不能仅凭 SDK 内包含 `openharmony` 目录认定工程满足赛事要求。

本文区分三类状态：

- **已配置**：工程文件中明确声明。
- **已安装**：本机安装元数据可核实。
- **已验证**：有对应构建、运行或真机验收证据。

已配置、已安装不等于已经通过整套兼容性验证。

## 2. 工程已配置项

| 配置项 | 当前值 | 依据 |
|---|---|---|
| 运行系统 | `HarmonyOS` | `build-profile.json5` 的 `runtimeOS` |
| 产品名称 | `default` | `build-profile.json5` 的 `products` |
| 兼容 SDK 配置 | `26.0.0` | `compatibleSdkVersion` |
| 目标 SDK 配置 | `26.0.0` | `targetSdkVersion` |
| 包配置 modelVersion | `26.0.0` | 根目录 `oh-package.json5` |
| Hvigor 配置 modelVersion | `26.0.0` | `hvigor/hvigor-config.json5` |
| 构建模式 | `debug`、`release` | `buildModeSet` |
| 构建任务入口 | `@ohos/hvigor-ohos-plugin` 的 `appTasks` | `hvigorfile.ts` |
| 应用模块 | `modules: []`，尚未注册 | `build-profile.json5` |
| 签名配置 | `signingConfigs: []`，尚未配置 | `build-profile.json5` |
| 工程依赖 | 当前声明的依赖为空 | 包配置与 Hvigor 配置 |

`modelVersion`、SDK 配置值、SDK 实际包版本、API Level 和工具版本分别记录，不相互替代。例如本工程的 `modelVersion` 为 `26.0.0`，并不意味着 Hvigor 程序版本也是 `26.0.0`。

## 3. 本机已安装与已记录版本

以下是当前开发机快照，不代表所有成员环境已统一。

| 项目 | 核实值 | 证据来源 |
|---|---|---|
| DevEco Studio | `26.0.0.821` | 安装目录 `product-info.json`，与项目 IDE 缓存一致 |
| IDE Build | `261.23567.138.36.2600821` | `product-info.json`、`build.txt` |
| SDK 名称与阶段 | `HarmonyOS 26.0.0`，`Release` | `sdk/default/sdk-pkg.json` |
| SDK 实际包版本 | `26.0.0.105` | SDK 元数据 `data.version` |
| SDK API Level | `26` | SDK 元数据 `data.apiVersion` |
| SDK Toolchains | `26.0.0.105`，API `26` | `openharmony/toolchains/oh-uni-package.json` |
| Hvigor | `6.26.4` | 安装包 `@ohos/hvigor/package.json`，既有报告亦记录此版本 |
| Hvigor OHOS 插件 | `6.26.4` | 安装包 `@ohos/hvigor-ohos-plugin/package.json` |
| OHPM | `26.0.0.630` | 安装目录 `tools/ohpm/package.json` |
| IDE 配套 Node.js | `24.14.1` | 配套 `node.exe` 文件版本，与项目既有同步报告一致 |
| 当前 PATH 中的 Node.js | `24.18.0` | PATH 解析到的 `node.exe` 文件版本 |
| IDE 配套 Java Runtime | `25.0.2+1-b329.117` | 安装目录 `jbr/release` |
| DevEco Code | 具体版本待核实 | 不根据 DevEco Studio 版本推断 |

本机存在两套不同版本的 Node.js。团队脚本和后续 CI 应明确实际使用的工具来源，避免 IDE 与终端无意间使用不同环境。

本机安装路径不作为成员必须采用的目录结构。IDE 缓存与构建报告只用于核验，不要求提交这些生成文件。

## 4. 当前工程与验证状态

| 项目 | 当前状态 |
|---|---|
| 工程骨架 | 已有根工程配置、Hvigor 入口、签名说明及忽略规则 |
| 应用模块与业务代码 | 尚未建立应用模块 |
| Stage 模型 | 属于架构建议，尚无模块配置可验证 |
| Git | 已初始化 Git 并建立基线提交；`dev/ux`、`dev/agent`、`dev/platform` 三个长期开发分支已建立 |
| specs 契约骨架 | 已建立 `specs/README.md` 与 `_模板.md`，尚无具体功能 spec |
| 许可证 | 根目录已采用 MIT License |
| 签名材料 | `signing/` 当前仅有说明文件，实际签名与安装待验证 |
| 依赖锁文件 | 核验时未发现根目录 `oh-package-lock.json5` |
| 既有工程同步 | 2026-09-10 的两份 Hvigor 报告均失败 |
| 失败原因 | `00303011 Configuration Error: No modules found`，与当前空模块配置一致 |
| 构建、安装与真机验收 | 本次未执行，当前不能标记为已通过 |

既有报告位于 `.hvigor/report/`，记录了 Hvigor `6.26.4` 与 Node.js `24.14.1`。这些是此前运行的证据，不是本次新执行的测试结果。

## 5. 待确认与能力验证

进入正式功能开发前逐项补齐：

| 待确认项 | 需要形成的结论 |
|---|---|
| 赛事平台 | 允许的系统发行版、SDK、设备、应用身份与交付格式 |
| 目标设备 | 型号、系统版本、API、屏幕形态及可用系统能力 |
| 开发工具 | 三人实际使用的 IDE、DevEco Code、SDK 与工具链版本 |
| 签名安装 | 证书、Profile、签名方式及目标设备安装结果 |
| LLM 接入 | 服务商、模型、多模态能力、流式协议、鉴权和额度 |
| 日历与提醒 | 权限范围、系统日历、代理提醒与通知跳转 |
| 文件与个人知识库 | 文件访问范围、离线存储、检索与问答运行条件 |
| 多模态与长语音 | 输入来源、录音/上传、识别与摘要能力及资源限制 |
| 多设备与协同 | 设备矩阵、登录身份、任务和上下文流转条件 |

验证用于明确实现方式与限制，不自动改变 00 中的需求优先级。需要调整需求时，由团队确认并同步文档。

## 6. 维护与升级约定

- 公共 SDK、modelVersion、构建工具或依赖升级单独评审，不随功能改动隐式升级。
- 配置变更与本文在同一 PR/MR 中更新，记录原因、影响和验证结果。
- 成员接入或 CI 建立时，对照本基线核验实际工具来源与版本。
- 后续版本验证至少覆盖工程同步、构建、签名安装及受影响的目标设备能力。
- 构建通过、模拟器通过和真机通过分别记录，不互相替代。
- 私有路径、密钥和签名密码通过本机配置管理，不写入本文或公共工程模板。
- 项目状态变化后及时替换过期结论，避免把启动阶段快照当作长期运行状态。
